import { sceneObjectActionTypes, ADDSCENEOBJECT, REMOVEALLSCENEOBJECT } from '../../actions/sceneObject/sceneObject';

const initState = {};

const reducer = (state = initState, action: sceneObjectActionTypes) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
